import * as BooksService from "./books.service";

describe("Like Test", () => {
  afterAll(async () => {});

  //TODO: createLike의 케이스들 나누어두기
  test("test", async () => {
    //expect(await BooksService.createLike(1, 1)).toEqual({ errorCode: 401 });
  });
});
